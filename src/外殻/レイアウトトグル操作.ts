// 外殻レイアウトのサイドバー/パネルの開閉操作を表す小さなインターフェース。
// メニューバー内蔵ボタン・独立トグルボタン（トグルボタン/配下）・アプリ側の任意のUIが、
// このインターフェース経由でシェルの表示状態を操作できる。
// 外殻レイアウト自身がこのインターフェースを実装するため、シェルのインスタンスを
// そのまま渡してもよいし、領域トグルサービスだけを渡してもよい。
export interface Iレイアウトトグル操作 {
    サイドバーを切り替える(): void;
    パネルを切り替える(): void;
}
